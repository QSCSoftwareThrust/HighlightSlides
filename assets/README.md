Place the official QSC logo here as:

```text
qsc-logo.png
```

The highlight deck generator prefers:

```text
qsc-logo-white.png
```

because the template places logos on a blue footer. This file is required:
deck generation fails if no QSC logo is available. Per-paper `Institution Logos`
should only list the institution(s) supported by QSC for that paper, not QSC
itself.
